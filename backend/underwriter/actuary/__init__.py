"""The Actuary — deterministic pricing and reserving.

FR-020: pure Python, no LLM calls. FR-027: no wall-clock, no randomness, no
network — the same snapshot must yield the same proposals forever. It computes;
it never persuades.
"""
